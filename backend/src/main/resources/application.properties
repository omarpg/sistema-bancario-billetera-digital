# ============================================================
# SERVER CONFIGURATION
# ============================================================
server.port=8080
spring.application.name=billetera-backend

# ============================================================
# DATABASE CONFIGURATION (SUPABASE)
# ============================================================
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.lqcupikzrrxzsfdizjmb
spring.datasource.password=hhwJqCzKl8TJQbFs
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool (HikariCP - viene por defecto con Spring Boot)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000

# ============================================================
# JPA / HIBERNATE CONFIGURATION
# ============================================================
# IMPORTANTE: validate porque el schema ya existe en Supabase
spring.jpa.hibernate.ddl-auto=validate

# Mostrar SQL en consola (útil para debug, desactivar en producción)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialecto de PostgreSQL
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# No crear schema automáticamente (ya lo tenemos)
spring.jpa.properties.hibernate.default_schema=public

# ============================================================
# LOGGING
# ============================================================
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.billetera.backend=DEBUG

# ============================================================
# SECURITY (temporalmente deshabilitado)
# ============================================================
# Por ahora deshabilitamos Spring Security para poder probar endpoints
# Lo configuraremos en la Fase 2
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
# ============================================================
# JWT CONFIGURATION
# ============================================================
jwt.secret=billetera_digital_secret_key_2026_debe_ser_muy_larga_al_menos_256_bits
jwt.expiration=86400000
# 86400000 ms = 24 horas